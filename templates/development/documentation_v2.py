# -*- coding: utf-8 -*-
"""
================================================================================
개발 문서화 프롬프트 V2.0 (Development Documentation Prompts V2.0)
================================================================================

## V2.0 핵심 개선사항

### 1. 동적 문서 섹션 생성 (Dynamic Section Generation)
- expected_issues를 필수 문서 섹션으로 직접 변환
- LLM이 각 섹션을 반드시 작성하도록 강제
- 비즈니스 V3.0에서 검증된 기법 적용

### 2. 5단계 누적 Chain-of-Thought
- STEP 1: 코드/시스템 이해
- STEP 2: 핵심 요소 추출
- STEP 3: 문서 구조 설계
- STEP 4: 문서 작성
- STEP 5: 완성도 검토

### 3. 서브카테고리별 전문가 페르소나
- api: API 문서 전문가 (Stripe/Twilio 스타일)
- readme: 오픈소스 문서 전문가
- comments: 코드 문서화 전문가
- architecture: 소프트웨어 아키텍트

### 4. 언어별 문서화 컨벤션
- Python: Google Style Docstring
- JavaScript/TypeScript: JSDoc/TSDoc
- Java: Javadoc
- Go: GoDoc
- Rust: Rustdoc

## 평가 기준 최적화
- issue_detection_rate * 4 (최대 4점) → 동적 섹션으로 극대화
- has_code_block (2점) → 예시 코드 반드시 포함
- has_structure (2점) → 테이블, 섹션 구조 강제
- has_specific_suggestions (2점) → 구체적 예시 강제

================================================================================
"""

from typing import List, Optional


# ============================================================================
# 공통 문서 섹션 생성 함수
# ============================================================================

def _build_doc_checklist(expected_elements: List[str]) -> str:
    """expected_elements를 문서 체크리스트로 변환"""
    if not expected_elements:
        return "- 일반적인 문서 구성 요소"

    checklist_items = []
    for i, element in enumerate(expected_elements, 1):
        checklist_items.append(f"| {i} | **{element}** | 📝 작성 필요 |")

    return "\n".join(checklist_items)


def _build_doc_sections(expected_elements: List[str]) -> str:
    """expected_elements를 문서 섹션으로 변환 (동적 섹션 생성)"""
    if not expected_elements:
        return """
### 필수 섹션 1: 개요
[내용 작성]

### 필수 섹션 2: 사용법
[내용 작성]
"""

    sections = []
    for i, element in enumerate(expected_elements, 1):
        sections.append(f"""
### 필수 섹션 {i}: {element}
[{element}에 대한 상세 내용 작성]
""")

    return "\n".join(sections)


# ============================================================================
# API 문서화 V2.0
# ============================================================================

API_DOCUMENTATION_V2_TEMPLATE = """## 역할: 시니어 API 문서 전문가

당신은 Stripe, Twilio 수준의 개발자 친화적 API 문서를 작성하는 전문가입니다.

**나의 3가지 원칙:**
1. "개발자가 5분 안에 첫 API 호출을 성공할 수 있어야 한다"
2. "모든 예외 상황을 문서화하라"
3. "복사-붙여넣기 가능한 예제를 제공하라"

**전문 분야**: OpenAPI/Swagger, RESTful 설계, API 문서 UX

---

## 문서화 대상

**API 이름**: {api_name}
**엔드포인트**: `{endpoint}`
**HTTP 메서드**: `{http_method}`
**API 목적**: {api_purpose}

**요청 파라미터**: {request_params}
**응답 형식**: {response_format}
**인증 방식**: {auth_method}

---

## 문서 체크리스트

⚠️ **중요**: 아래 모든 항목을 **반드시** 문서에 포함해야 합니다.

| 번호 | 필수 항목 | 상태 |
|------|----------|------|
{checklist}

---

## STEP 1: API 분석

### 핵심 기능
- **주요 기능**: [이 API가 하는 일]
- **사용 시나리오**: [언제 이 API를 사용하는지]
- **전제 조건**: [호출 전 필요한 것]

### 데이터 흐름
```
요청 → [검증] → [처리] → [응답]
```

---

## STEP 2: 핵심 요소 추출

### 요청 분석
- **필수 헤더**: [목록]
- **필수 파라미터**: [목록]
- **선택 파라미터**: [목록]
- **요청 본문 구조**: [JSON 스키마]

### 응답 분석
- **성공 응답 코드**: [200/201/204 등]
- **응답 본문 구조**: [JSON 스키마]
- **에러 응답 구조**: [에러 형식]

---

## STEP 3: 문서 구조 설계

문서는 다음 순서로 구성합니다:
1. 개요 (한 문장)
2. 인증 요구사항
3. 요청 형식 (헤더, 파라미터, 본문)
4. 응답 형식 (성공, 에러)
5. 예시 코드 (cURL, Python, JavaScript)
6. 에러 처리 가이드

---

## STEP 4: API 문서 작성

{doc_sections}

---

# {api_name}

## 개요
[API의 목적을 한 문장으로]

## 기본 정보
| 항목 | 값 |
|------|-----|
| Endpoint | `{endpoint}` |
| Method | `{http_method}` |
| 인증 | {auth_method} |
| Rate Limit | {rate_limit} |

## 인증
```
Authorization: Bearer YOUR_API_KEY
```

## 요청

### Headers
| Header | 필수 | 설명 |
|--------|------|------|
| Authorization | ✅ | Bearer 토큰 |
| Content-Type | ✅ | application/json |

### Parameters
| 파라미터 | 타입 | 필수 | 설명 | 예시 |
|---------|------|------|------|------|
| [param1] | string | ✅ | [설명] | `"example"` |
| [param2] | integer | ❌ | [설명] | `10` |

### 요청 예시

**cURL**
```bash
curl -X {http_method} '{endpoint}' \\
  -H 'Authorization: Bearer YOUR_API_KEY' \\
  -H 'Content-Type: application/json' \\
  -d '{{
    "param1": "value1"
  }}'
```

## 응답

### 성공 응답 (200 OK)
```json
{{
  "success": true,
  "data": {{
    "id": "abc123",
    "created_at": "2024-01-01T00:00:00Z"
  }}
}}
```

### 필드 설명
| 필드 | 타입 | 설명 |
|------|------|------|
| success | boolean | 요청 성공 여부 |
| data | object | 응답 데이터 |

## 에러 처리

### 에러 응답 형식
```json
{{
  "success": false,
  "error": {{
    "code": "ERROR_CODE",
    "message": "에러 설명"
  }}
}}
```

### 에러 코드
| 코드 | HTTP Status | 설명 | 해결 방법 |
|------|-------------|------|----------|
| INVALID_PARAM | 400 | 잘못된 파라미터 | 파라미터 형식 확인 |
| UNAUTHORIZED | 401 | 인증 실패 | API 키 확인 |
| NOT_FOUND | 404 | 리소스 없음 | ID 확인 |
| RATE_LIMITED | 429 | 요청 제한 초과 | 잠시 후 재시도 |

## 사용 예시

### Python
```python
import requests

response = requests.{http_method_lower}(
    '{endpoint}',
    headers={{'Authorization': 'Bearer YOUR_API_KEY'}},
    json={{'param1': 'value1'}}
)
print(response.json())
```

### JavaScript
```javascript
const response = await fetch('{endpoint}', {{
  method: '{http_method}',
  headers: {{
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  }},
  body: JSON.stringify({{ param1: 'value1' }})
}});
const data = await response.json();
```

---

## STEP 5: 문서 검토

### 완성도 체크리스트
- [ ] 모든 파라미터가 문서화되었는가?
- [ ] 모든 응답 필드가 설명되었는가?
- [ ] 에러 케이스가 모두 포함되었는가?
- [ ] 예시 코드가 실행 가능한가?

### 문서 품질 점수: [X/10]

---

## 검토 완료 확인

✅ API 분석 완료
✅ 필수 체크리스트 모든 항목 문서화 완료
✅ 예시 코드 제공 완료
✅ 에러 처리 가이드 완료
"""


# ============================================================================
# README 문서화 V2.0
# ============================================================================

README_DOCUMENTATION_V2_TEMPLATE = """## 역할: 오픈소스 문서 전문가

당신은 GitHub에서 10K+ 스타를 받은 프로젝트의 README를 작성한 경험이 있는 전문가입니다.

**나의 3가지 원칙:**
1. "5분 안에 설치하고 실행할 수 있어야 한다"
2. "스크롤 없이 프로젝트의 가치를 알 수 있어야 한다"
3. "복사-붙여넣기로 시작할 수 있어야 한다"

**전문 분야**: GitHub README, 기술 문서 UX, 개발자 온보딩

---

## 문서화 대상

**프로젝트명**: {project_name}
**한 줄 설명**: {one_liner}
**주요 기능**: {main_features}
**기술 스택**: {tech_stack}
**대상 사용자**: {target_users}

**코드/설정 예시**:
```{language}
{code_snippet}
```

---

## 문서 체크리스트

⚠️ **중요**: 아래 모든 항목을 **반드시** README에 포함해야 합니다.

| 번호 | 필수 항목 | 상태 |
|------|----------|------|
{checklist}

---

## STEP 1: 프로젝트 분석

### 핵심 가치
- **문제**: [이 프로젝트가 해결하는 문제]
- **해결책**: [어떻게 해결하는지]
- **차별점**: [다른 솔루션과의 차이점]

### 대상 독자
- **주요 사용자**: [누가 이 프로젝트를 사용하는지]
- **기술 수준**: [어느 정도 기술 수준을 가정하는지]

---

## STEP 2: 핵심 요소 추출

### 필수 정보
- **설치 요구사항**: [필요한 환경]
- **설치 방법**: [단계별 설치]
- **빠른 시작**: [가장 빠른 사용법]
- **설정 옵션**: [설정 가능한 항목]

### 추가 정보
- **API/메서드 목록**: [주요 기능]
- **예시**: [실제 사용 사례]
- **FAQ**: [자주 묻는 질문]

---

## STEP 3: 문서 구조 설계

README 구조:
1. 프로젝트명 + 뱃지
2. 한 줄 설명 (blockquote)
3. 주요 기능 (bullet points)
4. 빠른 시작 (Quick Start)
5. 설치 방법 (Installation)
6. 사용법 (Usage)
7. API 문서 (API Reference)
8. 예시 (Examples)
9. 기여 가이드 (Contributing)
10. 라이선스 (License)

---

## STEP 4: README 작성

{doc_sections}

---

# {project_name}

![Build Status](https://img.shields.io/badge/build-passing-brightgreen)
![Version](https://img.shields.io/badge/version-1.0.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)

> {one_liner}

## 주요 기능

- [기능 1 설명]
- [기능 2 설명]
- [기능 3 설명]

## 빠른 시작

```bash
# 설치
[설치 명령어]

# 실행
[실행 명령어]
```

## 설치

### 요구 사항
- [요구 사항 1]
- [요구 사항 2]

### 패키지 매니저로 설치
```bash
[설치 명령어]
```

### 소스에서 빌드
```bash
git clone [repository]
cd {project_name}
[빌드 명령어]
```

## 사용법

### 기본 사용법
```{language}
[기본 코드 예제]
```

### 고급 사용법
```{language}
[고급 코드 예제]
```

## 설정

| 옵션 | 타입 | 기본값 | 설명 |
|------|------|--------|------|
| [option1] | string | "default" | [설명] |
| [option2] | boolean | true | [설명] |

## API Reference

### `mainFunction(param1, param2)`
[함수 설명]

**Parameters:**
- `param1` (string): [설명]
- `param2` (object): [설명]

**Returns:** [반환값 설명]

**Example:**
```{language}
[예시 코드]
```

## 예시

### 예시 1: [시나리오]
```{language}
[예시 코드]
```

### 예시 2: [시나리오]
```{language}
[예시 코드]
```

## 문제 해결

### 자주 묻는 질문

**Q: [질문 1]**
A: [답변]

**Q: [질문 2]**
A: [답변]

## 기여하기

기여를 환영합니다! [CONTRIBUTING.md](CONTRIBUTING.md)를 확인해주세요.

1. Fork the repository
2. Create your feature branch (`git checkout -b feature/amazing-feature`)
3. Commit your changes (`git commit -m 'Add amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

## 라이선스

이 프로젝트는 [MIT License](LICENSE) 하에 배포됩니다.

---

## STEP 5: 문서 검토

### 완성도 체크리스트
- [ ] 5분 안에 설치 및 실행 가능한가?
- [ ] 모든 필수 섹션이 포함되었는가?
- [ ] 예시 코드가 실행 가능한가?
- [ ] 문서가 최신 상태인가?

### README 품질 점수: [X/10]

---

## 검토 완료 확인

✅ 프로젝트 분석 완료
✅ 필수 체크리스트 모든 항목 문서화 완료
✅ 예시 코드 제공 완료
✅ 기여 가이드 완료
"""


# ============================================================================
# 코드 주석/Docstring V2.0
# ============================================================================

CODE_COMMENTS_V2_TEMPLATE = """## 역할: 코드 문서화 전문가

당신은 {language}의 공식 문서화 스타일 ({doc_style})에 정통한 전문가입니다.

**나의 3가지 원칙:**
1. "IDE 자동완성에서 모든 정보가 보여야 한다"
2. "파라미터와 반환값은 반드시 문서화한다"
3. "예외 상황도 문서화한다"

**전문 분야**: {doc_style}, 코드 문서화 자동화, API 문서 생성

---

## 문서화 대상

**언어**: {language}
**문서화 스타일**: {doc_style}
**코드 목적**: {code_purpose}

```{language_lower}
{code}
```

---

## 문서화 체크리스트

⚠️ **중요**: 아래 모든 항목을 **반드시** 문서화해야 합니다.

| 번호 | 필수 항목 | 상태 |
|------|----------|------|
{checklist}

---

## STEP 1: 코드 분석

### 구조 분석
- **클래스 수**: [N개]
- **함수/메서드 수**: [N개]
- **공개 API**: [목록]
- **내부 헬퍼**: [목록]

### 복잡도 분석
- **복잡한 로직**: [위치 및 설명]
- **주석이 필요한 부분**: [위치]

---

## STEP 2: 핵심 요소 추출

### 각 함수/메서드별 정보
1. **함수명**: [이름]
   - 목적: [무엇을 하는지]
   - 파라미터: [목록]
   - 반환값: [타입 및 설명]
   - 예외: [발생 가능한 예외]

2. **함수명**: [이름]
   - ...

---

## STEP 3: 문서화 스타일 적용

### {doc_style} 형식

{style_guide}

---

## STEP 4: 문서화된 코드 작성

{doc_sections}

---

## 문서화된 코드

```{language_lower}
[완전히 문서화된 코드]
```

---

## STEP 5: 문서화 검토

### 문서화 커버리지
| 항목 | 총 개수 | 문서화됨 | 커버리지 |
|------|---------|---------|----------|
| 클래스 | [N] | [N] | [100%] |
| 공개 함수 | [N] | [N] | [100%] |
| 파라미터 | [N] | [N] | [100%] |

### 문서 품질 점수: [X/10]

---

## 검토 완료 확인

✅ 모든 공개 API 문서화 완료
✅ 파라미터/반환값 문서화 완료
✅ 예외 상황 문서화 완료
✅ 예시 코드 포함 완료
"""


# ============================================================================
# 아키텍처 문서화 V2.0
# ============================================================================

ARCHITECTURE_DOC_V2_TEMPLATE = """## 역할: 소프트웨어 아키텍트 & 테크니컬 라이터

당신은 복잡한 시스템 아키텍처를 이해하기 쉽게 문서화하는 전문가입니다.

**나의 3가지 원칙:**
1. "다이어그램 하나가 천 마디 말보다 낫다"
2. "왜 이 결정을 했는지 기록하라"
3. "독자 수준에 맞춰 작성하라"

**전문 분야**: C4 모델, arc42, ADR (Architecture Decision Records)

---

## 문서화 대상

**시스템명**: {system_name}
**시스템 목적**: {system_purpose}
**주요 기능**: {main_features}
**기술 스택**: {tech_stack}

**시스템 구성요소**:
```
{components}
```

**외부 연동**: {external_systems}
**대상 독자**: {target_audience}

---

## 문서 체크리스트

⚠️ **중요**: 아래 모든 항목을 **반드시** 문서에 포함해야 합니다.

| 번호 | 필수 항목 | 상태 |
|------|----------|------|
{checklist}

---

## STEP 1: 시스템 분석

### 핵심 요구사항
- **기능 요구사항**: [목록]
- **비기능 요구사항**: [성능, 확장성, 가용성 등]

### 제약 조건
- **기술 제약**: [기존 시스템, 기술 스택]
- **비즈니스 제약**: [예산, 일정]

---

## STEP 2: 아키텍처 핵심 요소

### 주요 컴포넌트
1. **컴포넌트 1**: [역할 및 책임]
2. **컴포넌트 2**: [역할 및 책임]

### 데이터 흐름
1. [흐름 1]
2. [흐름 2]

### 핵심 설계 결정 (ADR)
| 결정 사항 | 선택 | 이유 | 대안 |
|----------|------|------|------|
| [결정 1] | [선택] | [이유] | [대안들] |

---

## STEP 3: 문서 구조 설계

아키텍처 문서 구조:
1. 개요 (Context)
2. 컨테이너 (Containers)
3. 컴포넌트 (Components)
4. 코드 (선택)
5. 배포 (Deployment)
6. 횡단 관심사 (Cross-cutting concerns)

---

## STEP 4: 아키텍처 문서 작성

{doc_sections}

---

# {system_name} 아키텍처 문서

## 1. 개요

### 1.1 목적
[시스템이 해결하는 문제와 제공하는 가치]

### 1.2 범위
[이 문서가 다루는 범위]

### 1.3 대상 독자
- {target_audience}

---

## 2. 시스템 컨텍스트

### 2.1 컨텍스트 다이어그램
```
┌─────────────────────────────────────────┐
│              외부 시스템                 │
└─────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│            {system_name}                 │
└─────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│              사용자                      │
└─────────────────────────────────────────┘
```

### 2.2 외부 인터페이스
| 시스템 | 프로토콜 | 용도 |
|--------|---------|------|
| [외부 시스템 1] | [HTTP/gRPC 등] | [용도] |

---

## 3. 컴포넌트 아키텍처

### 3.1 컴포넌트 다이어그램
```mermaid
graph TB
    A[컴포넌트 A] --> B[컴포넌트 B]
    B --> C[컴포넌트 C]
    B --> D[데이터베이스]
```

### 3.2 컴포넌트 상세

#### 컴포넌트 A
- **역할**: [역할 설명]
- **책임**: [책임 목록]
- **기술**: [사용 기술]
- **인터페이스**: [제공하는 인터페이스]

#### 컴포넌트 B
- **역할**: [역할 설명]
- **책임**: [책임 목록]
- **기술**: [사용 기술]
- **의존성**: [의존하는 컴포넌트]

---

## 4. 데이터 아키텍처

### 4.1 데이터 모델
```
[ERD 또는 데이터 구조]
```

### 4.2 데이터 흐름
```mermaid
sequenceDiagram
    participant C as Client
    participant A as API
    participant D as Database
    C->>A: 요청
    A->>D: 쿼리
    D-->>A: 결과
    A-->>C: 응답
```

---

## 5. 배포 아키텍처

### 5.1 배포 다이어그램
```
[배포 환경 다이어그램]
```

### 5.2 인프라 요구사항
| 구성요소 | 스펙 | 수량 |
|---------|------|------|
| [서버] | [스펙] | [N] |

---

## 6. 횡단 관심사

### 6.1 보안
- **인증**: [방식]
- **인가**: [방식]
- **암호화**: [방식]

### 6.2 성능
- **목표 응답 시간**: [N]ms
- **목표 처리량**: [N] req/s

### 6.3 가용성
- **목표 가용성**: [99.9%]
- **장애 대응**: [전략]

### 6.4 모니터링
- **메트릭**: [수집 항목]
- **로깅**: [로깅 전략]
- **알림**: [알림 조건]

---

## 7. 설계 결정 기록 (ADR)

### ADR-001: [결정 제목]
- **상태**: 승인됨
- **컨텍스트**: [왜 이 결정이 필요했는지]
- **결정**: [무엇을 결정했는지]
- **결과**: [이 결정의 결과]

---

## STEP 5: 문서 검토

### 완성도 체크리스트
- [ ] 모든 컴포넌트가 문서화되었는가?
- [ ] 데이터 흐름이 명확한가?
- [ ] 설계 결정 이유가 기록되었는가?
- [ ] 대상 독자 수준에 맞는가?

### 아키텍처 문서 품질 점수: [X/10]

---

## 검토 완료 확인

✅ 시스템 분석 완료
✅ 필수 체크리스트 모든 항목 문서화 완료
✅ 다이어그램 제공 완료
✅ 설계 결정 기록 완료
"""


# ============================================================================
# 언어별 문서화 스타일 가이드
# ============================================================================

STYLE_GUIDES = {
    "python": """
**Google Style Python Docstring:**
```python
def function_name(param1: str, param2: int = 0) -> bool:
    \"\"\"한 줄 요약.

    더 자세한 설명이 필요한 경우 여기에 작성합니다.
    여러 줄에 걸쳐 작성할 수 있습니다.

    Args:
        param1: 첫 번째 파라미터 설명.
        param2: 두 번째 파라미터 설명. 기본값: 0.

    Returns:
        반환값에 대한 설명.

    Raises:
        ValueError: 발생 조건 설명.
        TypeError: 발생 조건 설명.

    Example:
        >>> result = function_name("example", 10)
        >>> print(result)
        True
    \"\"\"
```
""",
    "javascript": """
**JSDoc 스타일:**
```javascript
/**
 * 한 줄 요약.
 *
 * 더 자세한 설명이 필요한 경우 여기에 작성합니다.
 *
 * @param {string} param1 - 첫 번째 파라미터 설명.
 * @param {number} [param2=0] - 두 번째 파라미터 설명 (선택).
 * @returns {boolean} 반환값에 대한 설명.
 * @throws {Error} 발생 조건 설명.
 * @example
 * const result = functionName('example', 10);
 * console.log(result); // true
 */
```
""",
    "typescript": """
**TSDoc 스타일:**
```typescript
/**
 * 한 줄 요약.
 *
 * @remarks
 * 더 자세한 설명이 필요한 경우 여기에 작성합니다.
 *
 * @param param1 - 첫 번째 파라미터 설명.
 * @param param2 - 두 번째 파라미터 설명.
 * @returns 반환값에 대한 설명.
 * @throws {@link Error} 발생 조건 설명.
 * @example
 * ```ts
 * const result = functionName('example', 10);
 * ```
 */
```
""",
    "java": """
**Javadoc 스타일:**
```java
/**
 * 한 줄 요약.
 *
 * <p>더 자세한 설명이 필요한 경우 여기에 작성합니다.
 * 여러 줄에 걸쳐 작성할 수 있습니다.</p>
 *
 * @param param1 첫 번째 파라미터 설명
 * @param param2 두 번째 파라미터 설명
 * @return 반환값에 대한 설명
 * @throws IllegalArgumentException 발생 조건 설명
 * @see RelatedClass#relatedMethod()
 * @since 1.0
 */
```
""",
    "go": """
**GoDoc 스타일:**
```go
// FunctionName 한 줄 요약.
//
// 더 자세한 설명이 필요한 경우 여기에 작성합니다.
// 여러 줄에 걸쳐 작성할 수 있습니다.
//
// param1은 첫 번째 파라미터입니다.
// param2는 두 번째 파라미터입니다.
//
// 반환값은 결과를 나타냅니다.
// 에러는 실패 시 반환됩니다.
//
// Example:
//
//	result, err := FunctionName("example", 10)
//	if err != nil {
//	    log.Fatal(err)
//	}
```
""",
    "rust": """
**Rustdoc 스타일:**
```rust
/// 한 줄 요약.
///
/// 더 자세한 설명이 필요한 경우 여기에 작성합니다.
///
/// # Arguments
///
/// * `param1` - 첫 번째 파라미터 설명.
/// * `param2` - 두 번째 파라미터 설명.
///
/// # Returns
///
/// 반환값에 대한 설명.
///
/// # Errors
///
/// 에러 발생 조건 설명.
///
/// # Examples
///
/// ```
/// let result = function_name("example", 10);
/// assert!(result.is_ok());
/// ```
```
"""
}


# ============================================================================
# 프롬프트 생성 함수
# ============================================================================

def get_api_documentation_prompt_v2(
    api_name: str,
    endpoint: str,
    http_method: str,
    api_purpose: str,
    expected_elements: List[str],
    request_params: str,
    response_format: str,
    auth_method: str = "Bearer Token",
    error_codes: str = "",
    rate_limit: str = "1000 requests/hour"
) -> str:
    """
    API 문서화 프롬프트 V2.0 생성

    핵심 개선: expected_elements를 동적 섹션으로 변환
    """
    checklist = _build_doc_checklist(expected_elements)
    doc_sections = _build_doc_sections(expected_elements)

    return API_DOCUMENTATION_V2_TEMPLATE.format(
        api_name=api_name,
        endpoint=endpoint,
        http_method=http_method,
        http_method_lower=http_method.lower(),
        api_purpose=api_purpose,
        request_params=request_params,
        response_format=response_format,
        auth_method=auth_method,
        error_codes=error_codes or "표준 HTTP 에러 코드",
        rate_limit=rate_limit,
        checklist=checklist,
        doc_sections=doc_sections
    )


def get_readme_prompt_v2(
    project_name: str,
    one_liner: str,
    main_features: str,
    tech_stack: str,
    expected_elements: List[str],
    code_snippet: str,
    installation: str = "",
    target_users: str = "개발자",
    language: str = "python"
) -> str:
    """
    README 문서 프롬프트 V2.0 생성

    핵심 개선: expected_elements를 동적 섹션으로 변환
    """
    checklist = _build_doc_checklist(expected_elements)
    doc_sections = _build_doc_sections(expected_elements)

    return README_DOCUMENTATION_V2_TEMPLATE.format(
        project_name=project_name,
        one_liner=one_liner,
        main_features=main_features,
        tech_stack=tech_stack,
        installation=installation or "pip install 또는 npm install",
        target_users=target_users,
        code_snippet=code_snippet,
        language=language,
        checklist=checklist,
        doc_sections=doc_sections
    )


def get_code_comments_prompt_v2(
    code: str,
    language: str,
    expected_elements: List[str],
    code_purpose: str = "",
    doc_style: str = ""
) -> str:
    """
    코드 주석/Docstring 프롬프트 V2.0 생성

    핵심 개선: expected_elements를 동적 체크리스트로 변환
    """
    # 언어별 기본 문서화 스타일
    default_styles = {
        "python": "Google Style",
        "javascript": "JSDoc",
        "typescript": "TSDoc",
        "java": "Javadoc",
        "go": "GoDoc",
        "rust": "Rustdoc"
    }

    if not doc_style:
        doc_style = default_styles.get(language.lower(), "표준 스타일")

    style_guide = STYLE_GUIDES.get(language.lower(), STYLE_GUIDES["python"])
    checklist = _build_doc_checklist(expected_elements)
    doc_sections = _build_doc_sections(expected_elements)

    return CODE_COMMENTS_V2_TEMPLATE.format(
        code=code,
        language=language,
        language_lower=language.lower(),
        code_purpose=code_purpose or "명시되지 않음",
        doc_style=doc_style,
        style_guide=style_guide,
        checklist=checklist,
        doc_sections=doc_sections
    )


def get_architecture_doc_prompt_v2(
    system_name: str,
    system_purpose: str,
    main_features: str,
    tech_stack: str,
    components: str,
    expected_elements: List[str],
    external_systems: str = "",
    data_flow: str = "",
    target_audience: str = "개발팀, 아키텍트",
    doc_level: str = "상세"
) -> str:
    """
    아키텍처 문서 프롬프트 V2.0 생성

    핵심 개선: expected_elements를 동적 섹션으로 변환
    """
    checklist = _build_doc_checklist(expected_elements)
    doc_sections = _build_doc_sections(expected_elements)

    return ARCHITECTURE_DOC_V2_TEMPLATE.format(
        system_name=system_name,
        system_purpose=system_purpose,
        main_features=main_features,
        tech_stack=tech_stack,
        components=components,
        external_systems=external_systems or "없음",
        data_flow=data_flow or "별도 설명 필요",
        target_audience=target_audience,
        doc_level=doc_level,
        checklist=checklist,
        doc_sections=doc_sections
    )
